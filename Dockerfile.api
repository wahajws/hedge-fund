FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

COPY server ./server

ENV NODE_ENV=production
ENV API_PORT=4000
ENV API_HOST=0.0.0.0

EXPOSE 4000

CMD ["npm", "run", "start:api"]
